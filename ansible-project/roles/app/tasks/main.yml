- name: Deploy application to Kubernetes
  shell: kubectl apply -f /opt/devops-project/k8s/
  environment:
    KUBECONFIG: /home/ubuntu/.kube/config

- name: Restart deployment
  shell: kubectl rollout restart deployment devops-app
  environment:
    KUBECONFIG: /home/ubuntu/.kube/config
